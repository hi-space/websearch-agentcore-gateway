# AWS Configuration
NEXT_PUBLIC_REGION=us-east-1
NEXT_PUBLIC_GATEWAY_ID=
NEXT_PUBLIC_GATEWAY_URL=
NEXT_PUBLIC_COGNITO_DOMAIN=
NEXT_PUBLIC_COGNITO_CLIENT_ID=

# Optional: for local testing
# NEXT_PUBLIC_API_URL=http://localhost:3000

# LLM judge (AgentCore Evaluation) — 선택적 배포. 1일 때만 playground에 "AI 품질 평가" 버튼 노출.
NEXT_PUBLIC_JUDGE_ENABLED=0
# 검색 품질 LLM 평가자 id (infra/scripts/create-evaluators.sh 가 출력). 미설정 시 해당 축은 평가 안 됨.
JUDGE_RELEVANCE_EVALUATOR_ID=
JUDGE_AUTHORITY_EVALUATOR_ID=
