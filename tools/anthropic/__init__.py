# Marks this directory as a regular package so local tests import the local
# handler instead of the same-named installed PyPI package (anthropic/firecrawl).
# Lambda packaging copies only handler.py, so this file is not deployed.
